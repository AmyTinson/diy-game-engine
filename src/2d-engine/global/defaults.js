import {ReactComponent as cuteCometIcon} from '../../assets/cuteCometIcon/cuteCometIcon.svg'

export const DEFAULT_ROOM_BACKGROUND_COLOR = 'transparent'
export const DEFAULT_PLAYER_STATE = {
  lives: 3,
  score: 0,
  position: [500,500],
  sprite: cuteCometIcon,
}